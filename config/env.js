/**
 * This module stores all the configurations data.
 * These configuration can be changed according to your choice
 */
module.exports = {
    'port': 3000, // server port
    'secret': 'this.is.really.secret',   //jwt secret
    'tokenExpireIn': 86400 // 24 hours
}